# =============================================================================
# ikabot-web - Environment Variables
# =============================================================================
# 1. Copy this file to .env
# 2. Change at least the values marked REQUIRED
# 3. Start the stack with: docker compose up -d --build
# =============================================================================

# -- Django / Hub -------------------------------------------------------------
DJANGO_SECRET_KEY=change-me-to-a-long-random-string
DJANGO_DEBUG=false
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,hub

# -- Initial admin user -------------------------------------------------------
ADMIN_CREATE=true
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@ikabot.local
ADMIN_PASSWORD=change-me-admin-password

# -- Database (MariaDB) -------------------------------------------------------
DB_NAME=ikabot_hub
DB_USER=ikabot
DB_PASSWORD=change-me-db-password
DB_HOST=mariadb
DB_PORT=3306
MYSQL_ROOT_PASSWORD=change-me-root-password

# -- Redis --------------------------------------------------------------------
REDIS_PASSWORD=change-me-redis-password
REDIS_URL=redis://:change-me-redis-password@redis:6379/0

# -- Shared secrets -----------------------------------------------------------
# APP_SECRET should be a stable application secret used by the hub.
APP_SECRET=change-me-app-secret
AGENT_TOKEN=change-me-agent-token

# -- Optional services --------------------------------------------------------
WEBSHARE_API_KEY=

# IkabotAPI provides captcha/token features (optional).
# Included in docker-compose as a profile: docker compose --profile captcha up -d
# The default URL works when using the built-in profile. Change only if self-hosting separately.
IKABOTAPI_URL=http://ikabotapi:5005

# -- Agent (agent_v2 container) -----------------------------------------------
HUB_URL=http://hub:8000
# Leave empty to auto-generate a unique ID on first start
AGENT_NODE_ID=
AGENT_NAME=ikabot-agent-01

# -- Optional Docker image tags -----------------------------------------------
HUB_IMAGE=blackoneal/ikabot-web-hub:latest
AGENT_IMAGE=blackoneal/ikabot-web-agent:latest

# -- Optional port overrides --------------------------------------------------
# HUB_PORT=8000
# DB_PORT=3306
# REDIS_PORT=6379
# PMA_PORT=8080
